document.addEventListener("keydown", (e) => {
  if (e.key === "ArrowLeft") {
    console.log("Left key is pressed.");
  }
  if (e.key === "ArrowRight") {
    console.log("Right key is pressed.");
  }
});
